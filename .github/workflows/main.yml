name: Build Vue
on: 
 workflow_dispatch:
 push:

 issues:
   types:
     - closed
permissions:
  actions: write
  attestations: write
  checks: write
  contents: write
  deployments: write
  id-token: write
  issues: write
  discussions: write
  packages: write
  pages: write
  pull-requests: write
  repository-projects: write
  security-events: write
  statuses: write
concurrency:
  group: "build"
  cancel-in-progress: true
jobs:
  build_vue:
    
    
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v4
   
  
    
          
   
    - id: Build-Vue
      uses: alphaleadership/VuePagesAction@1.1.4
      with:
        username: 'thomas-iniguez-visioli'
        reponame: 'blog-cine'
        token: ${{ secrets.TOKEN }} # Leave this line unchanged
    
   
