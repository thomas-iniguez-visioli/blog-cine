{
  "name": "hexo-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "rm dist/404.html  && cd dist &&git  commit -a -m'temp' && git pull && git push && cd .. &&npm run import && hexo generate",
    "import": "node prebuild.js>log.txt&&&&cd cdn && git add * */* &&git commit -m'update ",
    "clean": "hexo clean && npm run import",
    "deploy": "hexo deploy",
    "server": "hexo server"
  },
  "hexo": {
    "version": "7.3.0"
  },
  "dependencies": {
    "google-images": "^2.1.0",
    "google-img-scrap": "github:alphaleadership/google-img-scrap",
    "hexo": "^7.3.0",
    "hexo-generator-archive": "^2.0.0",
    "hexo-generator-category": "^2.0.0",
    "hexo-generator-index": "^4.0.0",
    "hexo-generator-tag": "^2.0.0",
    "hexo-renderer-ejs": "^2.0.0",
    "hexo-renderer-marked": "^7.0.0",
    "hexo-renderer-stylus": "^3.0.1",
    "hexo-server": "^3.0.0",
    "hexo-theme-landscape": "^1.0.0",
   
    "simple-git": "^3.27.0"
  }
}
